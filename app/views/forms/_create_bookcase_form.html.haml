= form_for("bookcase", url: bookcases_path, remote: true) do |f|
  = f.text_field :name, :class => "input-medium search-query span5"
  %button{:type => "submit", :class => "btn btn-inverse" }
    Create Bookcase