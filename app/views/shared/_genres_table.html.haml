%table.table.table-striped
  %thead
    %tr
      %th= "Genre"
  %tbody
    - genres.each do |genre|
      %tr
        %td= link_to genre.name, genre_path(genre)

= will_paginate @genres, :class => 'pager', renderer: BootstrapPagination::Rails
